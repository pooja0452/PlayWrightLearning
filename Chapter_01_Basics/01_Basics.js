console.log("Hello, World!");

let x=10;//let means creating a new variable
console.log(x);

console.log(process.platform);//process.platform is a built-in variable that returns the platform of the operating system

console.log(process.arch);//process.arch is a built-in variable that returns the architecture of the operating system

console.log("Node version :"+process.version);//process.version is a built-in variable that returns the version of Node.js